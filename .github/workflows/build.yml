name: Build G3SDK

on:
  push:
    branches: [ master, f/** ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 3.1.401
    - name: Install dependencies
      run: dotnet restore
    - name: Build debug
      run: dotnet build --configuration Debug
    - name: Build release
      run: dotnet build --configuration Release
      uses: DanaBear/code-sign-action@v4
      with:
        certificate: '${{ secrets.TOBIICODESIGNCERTIFICATE }}'
        password: '${{ secrets.CODESIGNPASSWORD }}'
        certificatesha1: '4e59a4c4d80616561740f68c71ba08978e84db78'
        certificatename: 'Tobii AB'
        folder: 'G3SDK/bin/Release'
        recursive: true
    - name: Upload nuget package
      uses: actions/upload-artifact@v2
      with:
        name: installer
        path: G3SDK/bin/Release/Tobii.Pro.G3.SDK.net.*.nupkg
    - name: Upload signed dll
      uses: actions/upload-artifact@v2
      with:
        name: binaries
        path: G3SDK/bin/Release/*.dll

